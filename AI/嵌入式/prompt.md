#### 给出 提示语

```
你没有按照我们的架构来，请满足下面的工程约束:
1.freertos.c为CubeMX/系统层文件，不直接承载用户业务任务创建
2.所有用户任务统一在 user_init.c/user_task.c中创建
3.新增串口输出任务必须接入现有用户任务启动链路
4.不允许绕过现有初始化架构
5.修改前先搜索项目中任务创建入口和user_init.c的调用关系
2.2工件:不要只给一句需求，要把“判断依据"也给它
具体落脚点:
freertos.c:系统层FreeRTOS初始化文件
user_init.c:用户初始化统一入口
user_task.c:用户任务集中创建文件
当前项目约定:所有用户任务都通过UserTask_CreateAll)创建
目标:新增一个串口输出任务，每秒打印一次hello
要求:优先复用已有任务启动框架，不要直接把用户任务创建散落到freertos.c
```

