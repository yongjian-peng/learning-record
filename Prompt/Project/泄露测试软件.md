## 使用组合架构

```
$env:PATH='E:\Qt\Qt5.12\Tools\CMake_64\bin;E:\Qt\Qt5.12\Tools\mingw1310_64\bin;E:\Qt\Qt5.12\6.10.2\mingw_64\bin;C:\ninja;' + $env:PATH
powershell -ExecutionPolicy Bypass -File 'E:\WorkSpace\QT-Github\Leak\LeakTestSystem\tools\build_qsqlmysql.ps1' -MySqlRoot 'E:\mysql-connector-c-6.1.11-winx64 编译 mysql 扩展命令

重新设计项目目录。长期维护目的。Qt Quick Controls 2
+
MVVM
+
自定义组件
参考架构 

阅读这个项目，参考 uwerat/qskinny 项目，
1. 设计项目，设计项目目录。目的长期维护目的。使用Qt Quick Controls 2 + MVVM + 自定义组件来实现，参考https://github.com/mavlink/qgroundcontrol 定义架构，解耦，后端业务和页面效果解耦，
 2. 四大块：PLC 控制， 触控接口， 传感器（温度，压力，流量）,数据库（Mysql）
 3. 设计合理的数据表结构。能够兼容页面中的逻辑，并给出 sql 实例，关于时间的字段使用 bigint 兼容时区.
4. 设计用户登录业务对应的编码。
5.  QT安装目录：E:\Qt\Qt5.12 QT 运行目录：E:\WorkSpace\QT-Github\QML-Examples-Collection-master ，使用的 构建设置 E:\WorkSpace\qskinny-build\build\Desktop_Qt_6_10_2_MinGW_64_bit-Debug 构建 Kit 用的是 Desktop Qt6.10.2 MinGW 64-bit, 需要的 cmake 也在 E 盘目录中E:\Qt\Qt5.12\Tools\CMake_64\bin 不用 C 盘找编译工具。 
6. 目前已经成功运行 demo 项目 QML-Examples-Collection-master  
7. 在 目录 E:\WorkSpace\QT-Github\Leak 中创建一个新的目录，直接编写需求。不用其他的地方新建一个文件夹编写，不用担心污染，修改错误，大胆的修改代码。效果是，能在基础上运行这个代码。
8.  样式不符合要求，完全和设计三张图片保持一致的。. 代码功能已经实现，梳理一份项目程序的流程图。和架构图。布局是按照上面两张图片中的图片比例，下面列表的布局也是按照上面的样式。 样式兼容，自适应布局


LEAK_DB_HOST=192.168.31.224
LEAK_DB_PORT=3306
LEAK_DB_NAME=leak_test_system
LEAK_DB_USER=root
LEAK_DB_PASSWORD=root 已经配置在 qtquickcontrols2.conf 文件中，数据库是存在的。然后项目打通数据的查询保存，修改，删除
当前 Qt 运行目录没有 QMYSQL 插件，E:\Qt\Qt5.12\6.10.2\Src 已经安装好了  Qt 工具中的 source , 编译需要的 扩展。并运行 在项目中使用 MySQL，
2. 看到使用 sql 语句时候，有相对好用的工具吗？这是原生的 sql, 而且也没有 关于 sql 注入的措施。优化



```





## 需求分析+数据库设计

```
参考 https://github.com/uwerat/qskinny 项目，结合实例，实现上述页面，
要求：
1. 先分析，并结合需求设计架构，解耦。按照中型项目设计。
2. 分析有哪些模块，拆解合理的模块，和项目目录和文件结构表。将领 qskinny 项目
2. 在项目上引入 使用 Mysql 的组件。
3. 结合给到的图片，设计图片中展示出来的数据库表结构，最好添加对应的数据。

初级表测试页面
次级表测试页面
实时曲线
查询列表
参数管理
用户管理
报警记录
标签打印
Excel/PDF导出
历史数据追溯

不适合直接改 Examples,
LeakTestSystem
│
├── app
├── ui
├── viewmodels
├── domain
├── services
├── workflow
├── communication
├── repository
├── database
├── resources
└── thirdparty
ui
│
├── MainWindow
│
├── pages
│
│   ├── DashboardPage
│   ├── PrimaryTestPage
│   ├── SecondaryTestPage
│   ├── RecipePage
│   ├── HistoryPage
│   ├── AlarmPage
│   ├── UserPage
│   └── SettingPage
│
└── widgets
widgets层

全部QSkinny组件化。

widgets
│
├── HeaderBar
├── SideMenu
├── StatusBar
│
├── PressureCard
├── TemperatureCard
├── DeltaPressureCard
│
├── AlarmBanner
│
├── TrendChart
│
├── RecipeTable
│
├── UserTable
│
└── ConfirmDialog
ViewModel层

MVVM。

viewmodels
│
├── DashboardVM
├── PrimaryTestVM
├── SecondaryTestVM
├── RecipeVM
├── AlarmVM
└── HistoryVM
Q_PROPERTY(double pressure)
Q_PROPERTY(double temperature)
Q_PROPERTY(QString state)

QSkinny控件只绑定数据。


```



## 项目设计架构

```

阅读这个项目，参考 uwerat/qskinny 项目，
1. 这个项目，使用QT 技术实现，分析，使用哪些技术实现。

技术架构分析。以及使用哪个框架合适。给出架构设计。解耦，拆解模块。
2. 结合文档需求。编写自测代码和业务逻辑代码。给出可以运行的代码。
按照 uwerat/qskinny 风格。按照中型和大型项目思想编写。
3. QT安装目录：E:\Qt\Qt5.12 QT 运行目录：E:\WorkSpace\qskinny-build ，使用的 构建设置 E:\WorkSpace\qskinny-build\build\Desktop_Qt_6_10_2_MinGW_64_bit-Debug 构建 Kit 用的是 Desktop Qt6.10.2 MinGW 64-bit, 需要的 cmake 也在 E 盘目录中E:\Qt\Qt5.12\Tools\CMake_64\bin 不用 C 盘找编译工具。
4. 目前已经成功运行 demo 项目 qskinny 
5. 在 目录 E:\WorkSpace\qskinny-build\examples 中创建一个新的目录，直接编写需求。不用其他的地方新建一个文件夹编写，不用担心污染，修改错误，大胆的修改代码。效果是，能在基础上运行这个代码。



结论先说清楚：当前仓库是 QSkinny UI 框架源码，不是泄漏测试业务软件本体。适合的做法是：不要改 QSkinny 核心库，而是在它上面新增一个独立的 LeakTester 应用层，把业务、设备通信、数据存储和界面彻底分开。
参考到的关键位置：
README.md
src/CMakeLists.txt
src/controls/QskControl.h
src/controls/QskSkinlet.h
examples/iotdashboard/main.cpp
泄漏测试软件要求(2).pptx
需求拆解
PPT 里的核心业务可以归纳为：
设备输入：环境温度，初级表 6 路压力，次级表 2 路压力，规格包含 0-5V / 4-20mA / CANopen
外设：PLC、阀门、触摸屏、工控机、显示器、打印机
管理功能：登录、管理员参数设置、配方新建/查询/更新
测试流程：选择配方，录入物料号/工单号，确认安装，开始测试，PLC 开阀，2 秒采样，曲线显示
判定逻辑：充气 10 秒，关阀保压 300 秒，测试 20 分钟，压差超过限值判定不合格
初级表额外流程：PRV 打开压力、关闭压力、回调输出压力需要人工录入/确认
结果处理：保存测试数据，打印序列号标签，历史查询，导出 Excel/PDF
安全提示：结束或中止后，如果压力传感器仍有压力，主界面报警提示放气后拆卸

推荐架构
按照中型项目，建议采用分层 + 端口适配器结构：

app/leaktester
  main.cpp
  AppBootstrap.cpp

  presentation/          # QSkinny 界面层
    MainWindow
    HomePage
    RecipePage
    TestPage
    HistoryPage
    LoginDialog
    widgets/

  application/           # 用例层，协调业务流程
    AuthService
    RecipeService
    TestSessionService
    ReportService
    PrintService
    AlarmService

  domain/                # 纯业务模型，不依赖 Qt UI 和硬件细节
    Recipe
    WorkOrder
    TestSession
    TestResult
    PressureSample
    TestStateMachine
    LeakJudgementPolicy
    SerialNumberRule

  infrastructure/        # 外部系统适配
    plc/
      IPlcClient
      CanOpenPlcClient
      MockPlcClient
    sensors/
      ISensorReader
      PressureSensorReader
      TemperatureSensorReader
    storage/
      IRepository
      SqliteRepository
    report/
      ExcelExporter
      PdfExporter
    printer/
      ILabelPrinter
      ZebraLabelPrinter 或 WindowsPrinterAdapter

  common/
    Result
    Clock
    Logger
    AppConfig
    
  依赖方向
最重要的解耦原则是：

presentation -> application -> domain
                         -> infrastructure interfaces

infrastructure -> application/domain interfaces 的实现

也就是说，界面不直接读 PLC，不直接写数据库，不直接判断合格与否。界面只调用 TestSessionService，订阅测试状态和采样数据。

核心状态机
测试流程建议显式建成状态机，避免按钮槽函数里堆业务判断：

Idle
 -> RecipeSelected
 -> WorkOrderEntered
 -> ProductConfirmed
 -> Filling             # 开阀充气 10s
 -> Holding             # 关阀保压 300s
 -> Testing             # 20min，2s 采样并判断压差
 -> Passed / Failed / Aborted
 -> PressureReleaseCheck
 -> Finished
 初级表可以在 Testing 前增加：
 PrvOpenCheck
PrvCloseCheck
ManualCallbackPressureInput

界面层建议
QSkinny 本身已经把控件、皮肤、渲染拆得比较干净，比如 QskControl 管控件行为，QskSkinlet 管渲染节点。业务软件应沿用这个思路：

HomePage：配方选择、物料号、工单号、开始按钮、压力未释放报警
RecipePage：管理员权限，新建/查询/更新配方
TestPage：实时曲线、环境温度、输入压力、压差、实际测试时间、继续/停止/打印序列号
HistoryPage：历史数据列表，按物料号、产品类型、日期、结果查询，导出 Excel/PDF
LoginDialog：角色控制，普通操作员不能改配方参数
界面组件只展示 ViewModel 数据，例如：

class TestViewModel : public QObject
{
    Q_OBJECT
    Q_PROPERTY(QString stateText READ stateText NOTIFY changed)
    Q_PROPERTY(double ambientTemperature READ ambientTemperature NOTIFY changed)
    Q_PROPERTY(double inputPressure READ inputPressure NOTIFY changed)
    Q_PROPERTY(double differentialPressure READ differentialPressure NOTIFY changed)
    Q_PROPERTY(bool passed READ passed NOTIFY changed)
};


数据模型
建议先固定这些核心表/对象：

Recipe：压力表型号、产品类型、输入压力、泄漏压力限值、PRV 设定值、PRV 开启值、PRV 关闭值、测试时间、序列号格式
WorkOrder：物料号、工单号、操作员、产品类型
TestSession：开始时间、结束时间、当前状态、配方快照、工单信息
PressureSample：时间戳、环境温度、输入压力、各通道压力、压差
TestResult：合格/不合格/中止、失败原因、退出原因、打印状态、导出状态
配方要做“快照保存”，不要只保存 recipe_id。否则以后配方被改了，历史测试记录会失真。

硬件适配
PLC、传感器、打印机都建议抽接口：

class IPlcClient {
public:
    virtual ~IPlcClient() = default;
    virtual bool connectDevice() = 0;
    virtual bool openValve() = 0;
    virtual bool closeValve() = 0;
    virtual bool alarmOn(QString reason) = 0;
    virtual bool alarmOff() = 0;
};

class ISensorReader {
public:
    virtual SensorSnapshot readSnapshot() = 0;
};


中型项目落地顺序
建议分 4 步走：

先建 LeakTester 独立应用目标，复用 QSkinny，不修改 src 核心库。
先实现 domain + application + mock infrastructure，把完整测试流程跑通。
再接 SQLite、Excel/PDF、打印机。
最后接 PLC/CANopen/传感器，并补设备异常、断线重连、压力未释放报警。
关键建议
这个项目最容易失控的地方不是 UI，而是“测试流程 + 硬件 IO + 数据保存”混在一起。中型项目的边界应该是：

QSkinny 只负责显示和交互
application 负责流程编排
domain 负责判定规则
infrastructure 负责真实设备、数据库、导出、打印
状态机是测试流程的唯一入口
下一步最值得做的是先搭一个 app/leaktester 骨架：CMake 目标、领域模型、测试状态机、Mock PLC/传感器，以及一个最小 QSkinny 主界面。这样架构就能从纸面落到可运行代码上。

```

